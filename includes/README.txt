This directory, includes/, is used as a place to store 
SerialUI header files that are conditionally included
in the project.

Currently, these are the various localized string 
resource files and the platform-specific (serial 
stream) implementations.

